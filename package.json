{
  "name": "url-shortner",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anurag",
  "license": "ISC",
  "description": "A robust and scalable URL shortening service built with Node.js, Express.js, and MongoDB. This application allows users to create shortened URLs with optional custom codes and provides analytics for each shortened link.",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "mongoose": "^8.16.5"
  }
}
